import type { CollectionIcon } from "@/types/collection";

// Icon selector order (ids map to the folder glyphs in collection-glyphs.tsx).
export const collectionIconOptions: CollectionIcon[] = [
  "folder",
  "briefcase",
  "folderOpen",
  "tray",
  "book",
  "bookmark"
];
